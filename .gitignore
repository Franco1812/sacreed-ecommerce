# artefactos viejos, superados por web/ + api/ (ver vault del proyecto) — no se versionan
/sacred/
/frontend-mock/
/docker/
/docker-compose.yml

# por si algún día se corre algo en la raíz
/node_modules
.DS_Store
